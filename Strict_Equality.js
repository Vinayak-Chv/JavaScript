//Same as == != but we use === !== to check whether the value as well as DataType of the variable is same or not.

const pi = 3.14;

if (pi === 3.14) {
    console.log("That's a PI");
}
else {
    console.log("That's Not a PI");    
}